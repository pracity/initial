 -- user = root , password=Passoword
--create database pracity 
-- use pracity

create table product (p_id int not null AUTO_INCREMENT, p_code varchar(10), p_desc varchar(50), p_category int , primary key(p_id));